# Stack 1 - Questions

1. Can both the cloud {} and backend {} nested blocks be used at the same time?
2. How does Terraform interact with the target Application Programming Interfaces (APIs) when the code is executed?
3. Is it OK to code an empty backend {} block when writing the configuration if details are unknown?
4. What are the different options supported by the lifecycle meta-argument?
5. What are the different types of providers in Terraform?
6. What are the most fundamental building blocks of Terraform?
7. What are the three stages in the core Terraform workflow?
8. What are the two ways to configure Terraform Cloud?
9. What basic Terraform workflow commands are used in general?
10. What is the default count of the parallel operations that can be performed in a machine running the Terraform tool?
11. What is the host domain name to be used with Terraform Cloud?
12. What is the main purpose of the terraform plan command?
13. What is the name of the dependency lock file created by Terraform to track each dependency?
14. What is the naming convention used to set variable values when using the environment variables option?
15. What subcommands are supported by the terraform state command?
16. When the terraform init command is executed, the relevant providers will be downloaded into which directory?
17. Which backend is the default one supported by Terraform when no backend {} block is configured?
18. Which block will contain the Terraform settings along with the providers that will be used in the configuration?
19. Which command can be used to experiment with the Terraform expressions before the actual logic in an interactive console fashion?
20. Which command can be used to import the existing real-world infrastructure resources so they can be managed by Terraform?
21. Which command is used to format the configuration files to a canonical format and style specified by HashiCorp?
22. Which meta-argument can be used in resource blocks to define explicit dependency?
23. Which meta-argument should be used to create more than one identical resource with the same resource definition?
24. Which option of the terraform init command will result in migrating the state to the new backend without any user prompts?
25. With the terraform apply and terraform destroy commands, which option skips user prompts and proceeds with the command execution?
