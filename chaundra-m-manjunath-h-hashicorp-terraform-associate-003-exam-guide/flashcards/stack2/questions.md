# Stack 2 - Questions

1. A ______________ can be used to assign a name to the expression and can be used multiple times within the module.
2. At present, the dependency lock file tracks only the provider dependencies and does not remember the versions of the remote modules. True or false?
3. How do you apply custom rules or validations for the resources, data blocks, or outputs before or after the creation time?
4. How do you apply refresh-only mode in Terraform via the CLI to sync/update the Terraform state file to reconcile with the remote objects?
5. Is it true that the built-in Terraform functions will accept a varying number of arguments?
6. List the possible values for the TF_LOG environment variable in the order of decreasing verbosity of the logs.
7. Other than the default HCL syntax, Terraform can also support alternative syntax in ____________ configuration.
8. The ______________ function can be used to read the file in the given path and render the content using the supplied set of values.
9. The terraform console command holds a lock on the state file, and other operations cannot be performed on the state until it is released. True or false?
10. What are the maximum and minimum values of the base that can be specified with the parseint function? Syntax: parseint(input string, base number)
11. What are the structural types supported by Terraform to store values of multiple types under a single value?
12. What are the three primitive data types supported in Terraform?
13. What are the two main high-level parts of the resource address in Terraform?
14. What are the valid operators to use in the version constraints string in the terraform {} block?
15. What complex types are supported in Terraform?
16. What is a splat expression?
17. When new modules are used in the configuration for the first time, the modules will be downloaded into which directory?
18. Which attribute in a variable declaration will prevent setting the variable value as null?
19. Which AWS service is supported by Terraform as a valid source type for modules?
20. Which command can be used to download and update the modules mentioned in the root directory of the configuration?
21. Which environment variable in Terraform can be used to enable the logs generated to appear in the stderr terminal?
22. Which provider in Terraform can integrate and use the AWS secrets stored externally in the HashiCorp Vault instance?
23. Which Terraform command can be used to manually remove the lock on the state file?
24. Which Terraform offering by HashiCorp provides advanced features such as audit logging and SAML SSO integration?
25. Which Terraform plans/offerings include a private module registry for sharing modules internally within the organization?
